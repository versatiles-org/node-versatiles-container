{
  "name": "node-versatiles",
  "private": true,
  "workspaces": [
    "versatiles-container",
    "versatiles-google-cloud",
    "versatiles-release-tool",
    "versatiles-server"
  ],
  "eslintConfig": {
    "root": true
  },
  "scripts": {
    "build-deps": "npm run build -w @versatiles/container",
    "build": "npm run build -ws --if-present",
    "check": "npm run lint && npm run test && npm run build",
    "doc": "npm run build -w @versatiles/release-tool && npm run doc -ws --if-present",
    "lint": "npm run build-deps && eslint . --color",
    "test-coverage": "npm run build-deps && NODE_OPTIONS=--experimental-vm-modules jest --coverage",
    "test": "npm run build-deps && NODE_OPTIONS=--experimental-vm-modules jest",
    "upgrade": "npm-check-updates -u -ws --root && rm -f package-lock.json; rm -rf node_modules */node_modules; npm i -ws --force --include-workspace-root && npm update -ws --force --include-workspace-root"
  },
  "type": "module",
  "devDependencies": {
    "@types/jest": "^29.5.10",
    "@types/node": "^20.10.2",
    "@typescript-eslint/eslint-plugin": "^6.13.1",
    "@typescript-eslint/parser": "^6.13.1",
    "eslint": "^8.55.0",
    "jest": "^29.7.0",
    "npm-check-updates": "^16.14.11",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "tsx": "^4.6.2",
    "typescript": "^5.3.2"
  }
}
